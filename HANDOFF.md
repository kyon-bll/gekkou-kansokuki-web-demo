# Codex引き継ぎ — 月光観測記 Webデモ 0.2.0

## 残る作業

この `index.html` を初稿の最新版として使い、実際のURLでの最終確認・必要最小限の修正・ユーザーが指定するGitHub Pagesへの公開を行ってください。旧版のHTML・旧チュートリアル用のテストと混ぜないでください。

単一HTMLで動く構成です。CSS・JavaScript・SVGを内蔵し、外部通信・ライブラリ・npm・ビルド・バックエンドは不要です。ゼロからの作り直しや、整理のためだけのフレームワーク導入はしません。`.nojekyll` は公開時の補助で、ゲーム本体ではありません。

## 今回反映したこと

脚本原案と、この会話で承認された `TUTORIAL-REDESIGN.md` に基づき、既存の背景・探索・本の詳細・所持・移動・手帳・会話録を再利用しています。

- おじさん本人の登場・片付け許可ゲート・時系列パズル・「並びを読む」判定を削除。行方不明のおじさんの蔵書を整理する導入に変更。
- 床の『悪魔信仰』を手に取るとぷぎゅを救出。露出した紙をプレイヤーが調べ、配架表を発見する。
- 配架表は手帳と棚から再参照でき、棚では非モーダル表示のまま配置可能。開閉で本の選択を解除しない。
- 誤配架・床／机／脇への移動・回収を許容。四冊が揃うと自動で一度だけ棚が反応し、『月光観測記』が現れる。
- 日記を自分で開いて読む→本を閉じる→独白→終了。未読で閉じても再開でき、読了後は手帳から再読できる。

原案の最初の日記は原文どおりです。具体的な配架表・四冊の採用・『星辰の書』・追加会話・日記をこの棚で見せるタイミングは、承認済みのデモ用仮稿です。中盤・終盤の重要設定は追加していません。内容差し替え箇所は `GAME_DATA` にまとまっています。

## 短い確認経路（解答を含む）

「はじめる」→ ADV → カマキリ → 灯りのほうへ → 扉を調べて入室 → 床の本を「手に取る」→ ぷぎゅ登場 → 「紙の端」→ 配架表 → 窓辺の棚。

左から **悪魔信仰 → 星辰の書 → 妖の秘術 → エイボンの書**。

最初から2番目にある『エイボンの書』は4番目へ置き直します。表・全冊の本文を読んだことは、棚が反応するための隠れた条件ではありません。棚の反応後、「棚から現れた本」→「開いて読む」→ 本を閉じる → 独白で終了します。

## 確認済み／未確認

**実施済み：** Nodeのコードテスト22件成功。Chromiumで変更箇所を含む短い操作確認も成功。誤配架・表の開閉・回収・終了・再読・リセットを確認し、JavaScript例外・コンソールエラー・外部通信はありませんでした。詳細は `tests/RESULTS.md`。

**確認方法の制約：** この環境では `file://` への直接遷移が管理ポリシーでブロックされました。そのため、変更していないHTML全文をChromiumへ `page.set_content` で読み込んで操作を確認しています。実URLでの配信確認とは別です。

**未確認：** ファイル直接起動、HTTP配信、GitHub Pages公開、Safari／Firefox、通常速度のプレイ時間・読み心地。公開先・公開範囲・権限はユーザーの指定を確認し、既存リポジトリの公開範囲を勝手に変更しないでください。

## 確認コマンド（開発時のみ）

```sh
node --test tests/core.test.cjs
# Python Playwright と Chromium が既にある環境だけで使用
python tests/smoke.py --url "実際の確認先URL" --browser "/path/to/chromium"
```

新しいテスト基盤を導入する必要はありません。Computer Useは文章を早送りした軽い確認に留め、長時間の反復プレイはしないでください。確認できた範囲と公開URLを報告し、未確認のものを「公開済み」「動作確認済み」としないでください。
