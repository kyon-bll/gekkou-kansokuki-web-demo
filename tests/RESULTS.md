# 動作確認結果 — 0.2.0

実施日：2026-09-16。前版の結果の転記ではなく、この修正版に対する実行結果です。

## コードテスト

`node --test tests/core.test.cjs`：**22件成功・失敗0件**。

既存の所在・重複防止・誤配架・容量制限なしの移動テストを再利用し、書名・配架順・初期配置を更新。ぷぎゅの救出が最初の移動で一回だけ発生すること、失敗した移動では発生しないこと、全24配置のうち正解が一つであること、配架表未取得・本文未読でも棚が反応すること、完成後に置き直しても報酬を失わず反応が重複しないこと、リセットを確認しました。

## 短いブラウザ確認

Python Playwright / Chromium **144.0.7559.96**。修正版HTML全文を改変せず `page.set_content` で読み込み、会話は早送りして操作しました。

- タイトル、導入ADV、カマキリ、無人の倉庫、書庫への移行。
- 通常の「手に取る」でぷぎゅ救出。紙の露出、明示的な発見、床に本を戻しても紙が隠れないこと。
- 配架表を表示したまま選択・配置。表の開閉で選択を保持。占有位置への上書き防止、誤配架。
- 床・脇の棚からの回収と置き直し。最後の配置で魔法陣が表示され、判定ボタンなしで反応。
- 日記を未読で閉じる→再び開く→読む→閉じる→独白→デモ終了。
- 表と日記の手帳再読、会話録。完成後に本を動かしても報酬が残り、救出と棚反応の会話は各一回のみ。
- リセット。1365×900を中心に操作し、1365×768・390×844では同じ棚画面の横方向のはみ出しと表の表示を確認（各サイズでの全編操作確認ではありません）。

JavaScript例外 **0件**、コンソールエラー **0件**、通信要求 **0件**。

確認中に見つかった魔法陣SVGの表示属性の問題は修正し、再確認済みです。魔法陣のアニメーションも、完成後の再訪で繰り返さないようにしています。

## 未確認

`file://` への直接遷移は管理ポリシーでブロックされたため、ファイル直接起動は未確認です。HTTPの実URL配信・GitHub Pages公開・Safari／Firefox・実機スマートフォン・通常速度での所要時間や面白さも未確認です。

`set_content` による操作成功を、実URL配信や公開の確認として扱わないでください。
